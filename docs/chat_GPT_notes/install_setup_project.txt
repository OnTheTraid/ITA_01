Полный функционал setup_project.ps1
1. 🧱 Приветствие и информация

Выводит в консоль баннер:

ITA Trading Agent - Project Setup


чтобы разработчик понимал, какой проект он настраивает.

2. 🐍 Проверка Python

Проверяет наличие установленного Python.

Отображает текущую версию Python.

Если версия не 3.11.x, предупреждает и предлагает пользователю подтвердить продолжение:

WARNING: Expected Python 3.11.x, but found 3.10.6
Continue anyway? (y/n)

3. 🔍 Проверка виртуальной среды

Проверяет наличие папки:

.\venv\Scripts\python.exe


Если не найдена, сообщает ошибку и предлагает создать:

python -m venv venv


Если есть, сообщает:

Virtual environment found!

4. ⚡ Активация venv

Автоматически активирует виртуальную среду:

& .\venv\Scripts\Activate.ps1

5. 📦 Обновление pip

Обновляет pip до последней стабильной версии.

Проверяет, что обновление прошло успешно.

6. 📁 Проверка и создание базовой структуры проекта

Проверяет наличие ключевых директорий (в т.ч. logs/coredata).

Если нет — создаёт автоматически:

Created folder: logs/coredata
✅ Folder structure verified.

7. ⚙️ Проверка и установка config.yaml

Проверяет наличие файла config.yaml.

Если нет — сообщает ошибку.

Если файл есть, выводит:

config.yaml already exists.

8. 🔐 Проверка .env

Проверяет наличие .env файла.

Если нет, но есть .env.example → создаёт копию:

Creating .env from .env.example...
✅ .env created. Please edit it with your credentials!


Если .env уже есть → пишет:

.env file exists!

9. 🧩 Установка зависимостей

Проверяет наличие requirements.txt.

Устанавливает все библиотеки (с точными версиями).

При ошибке установки — выводит:

ERROR: Failed to install dependencies!

10. 🛰️ Настройка Prefect

Создаёт папку:

D:\ITA\ITA_01\.prefect


Создаёт системную переменную:

PREFECT_HOME = D:\ITA\ITA_01\.prefect


Применяет её сразу в текущей сессии PowerShell (без перезапуска).

Настраивает Prefect API:

http://127.0.0.1:4200/api


Создаёт папку:

D:\ITA\ITA_01\.chromadb


для векторной памяти GPT.

Выводит:

✅ Prefect & ChromaDB directories configured!

11. 🔒 Фиксация зависимостей (requirements.lock)

Проверяет, существует ли файл requirements.lock.

Если нет → создаёт его автоматически:

Lock file not found. Creating new requirements.lock...
✅ Lock file created and saved as requirements.lock


Если есть → сверяет установленные пакеты с ним:

При несовпадении показывает отличия:

⚠️  Version mismatch detected!


Предлагает выровнять окружение:

Do you want to re-sync environment to match lock file? (y/n)


Если выбрано y, выполняет:

pip install -r requirements.lock

12. 🧠 Проверка Prefect

Проверяет установленную версию Prefect:

Prefect version: 2.19.9
API version: 0.8.4
Database: SQLite

13. ✅ Финальная проверка

Выводит итоговый отчёт:

Setup Complete! Final Check:
✅ Python version: 3.11.9
✅ Prefect version: 2.19.9
✅ Key packages installed: loguru, pandas, MetaTrader5, prefect, PyYAML

14. 🧭 Подсказки по следующим шагам

В конце выводит понятные инструкции:

1. Edit .env file with your credentials (if not done yet)
2. Start Prefect server: prefect server start
3. Open Prefect UI: http://localhost:4200
4. Run your first flow from /flows directory

💾 Итого

Скрипт setup_project.ps1 теперь:

✅ Проверяет Python и виртуальное окружение

✅ Настраивает Prefect и ChromaDB

✅ Устанавливает все зависимости

✅ Создаёт requirements.lock

✅ Фиксирует версии библиотек

✅ Проверяет .env

✅ Показывает статус проекта и рекомендации