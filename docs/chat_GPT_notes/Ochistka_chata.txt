Я очищаю только визуальный буфер сессии — уберу тяжёлые куски (изображения, большие листинги кода, промежуточные выводы),
но все данные проекта ITA —

📂 ITA Docs,

🧩 Dependency Manifest,

🧠 Canvas,

⚙️ логика связей и модулей (CoreData, MT5Connector, SetupManager и т.д.)

— остаются нетронутыми и активными.


длинную текстовую историю диалога,

временные системные буферы,

неиспользуемые промежуточные объекты (коды, черновики, лог-сообщения и т. п.).

После этого у нас останется «чистая» и быстрая рабочая сессия, но с полным контекстом проекта ITA.