минимальный пример, как SetupManager (или Prefect FlowController) будет вызывать твой mt5_connector.
Этот пример можно вставить позже в модуль 01_SetupManager/setup_manager.py,
когда мы его будем проектировать.

🧩 Пример кода — интеграция SetupManager → MT5 Connector
# ===============================================================
# ITA Project | Module: SetupManager (1.1)
# Example: запуск коннектора CoreData (MT5)
# ===============================================================

from src.CoreData.mt5_connector import main as mt5_main
from datetime import datetime, timedelta

def run_data_ingest():
    """
    Управляемая точка входа SetupManager для MT5 Connector.
    Определяет дату, актив, режим — и передает в CoreData.
    """
    # === 1. Автоматически определяем диапазон (например, последние 30 дней)
    date_to = datetime.now().strftime("%Y-%m-%d")
    date_from = (datetime.now() - timedelta(days=30)).strftime("%Y-%m-%d")

    # === 2. Задаем торговый актив и таймфрейм
    symbol = "EURUSD"
    timeframe = "M15"

    # === 3. Определяем режим (взято из .env, но можно задать вручную)
    mode = "BACKTEST"

    # === 4. Вызываем коннектор CoreData
    mt5_main(
        mode=mode,
        symbol=symbol,
        timeframe=timeframe,
        date_from=date_from,
        date_to=date_to
    )

if __name__ == "__main__":
    run_data_ingest()

🧠 Что делает этот код — объяснение по шагам
🔹 1. SetupManager теперь решает, какой период загрузить

Он сам вычисляет date_from и date_to.
Например:

“Возьми последние 30 дней данных перед сегодняшним днём”.

Это гибко — SetupManager может изменять диапазон в зависимости от задач:

при первом запуске — загрузить весь архив (2020-01-01 → now);

при ежедневном обновлении — только последние сутки.

🔹 2. SetupManager указывает актив и таймфрейм

Это может быть EURUSD, XAUUSD, DE40 и т.д.
Причём он может передавать список активов и вызывать коннектор несколько раз — один раз на каждый инструмент.

🔹 3. SetupManager решает, какой режим работы использовать

"LIVE" → реальные данные с EquityEdge

"BACKTEST" → исторические данные с MetaQuotes

Он может даже переключать режим динамически — например, при ночном тестировании использовать BACKTEST, а днём LIVE.

🔹 4. CoreData теперь ничего не решает само

mt5_connector — это просто исполнитель:

подключается;

загружает данные;

проверяет целостность;

сохраняет их.

Вся логика “что, когда и зачем загружать” уходит вверх — в SetupManager.

✅ Что это даёт проекту ITA
Преимущество	Описание
Централизация логики	Всё управление датами и символами находится в одном месте — SetupManager.
Гибкость	Можно управлять временными диапазонами программно (например, через Prefect Flow).
Надёжность	Коннектор не может “перепутать даты” — он получает готовый диапазон.
Соответствие архитектуре ITA	Это именно то, что у нас прописано в ITA_Dependency_Manifest.yaml — SetupManager управляет входами всех Core-модулей.