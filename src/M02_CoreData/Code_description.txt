
======================
mt5_connector.py
Что делает 
Функция	Назначение
connect_mt5()			- Подключается к EquityEdge или MetaQuotes Demo без fallback
clear_cache()			- Очищает временные .parquet файлы в data/cache/
schedule_cache_cleanup()	- Запускает автоматическую очистку при запуке лайв и потом каждые 60 минут
download_data()			- Загружает котировки и сохраняет в archive/ или cache/
main()				- Управляет полным циклом работы: init → загрузка → завершение
ITA_MODE			- Управляет режимом работы (LIVE / BACKTEST) через .env

🧩 Итоговая структура проекта
data/
├── archive/raw/EURUSD/M15/2025_01_01_2025_11_01.parquet
└── cache/EURUSD_M15_live.parquet

logs/coredata/mt5_connector.log
.env
config.yaml
src/02_CoreData/mt5_connector.py

✅ Проверка

Установи ITA_MODE=LIVE в .env → создаётся cache, автоочистка каждые 1 ч.

Установи ITA_MODE=BACKTEST → архивируются исторические данные.

В logs/coredata/mt5_connector.log всё детально логируется.
-----
Краткое описание функций MT5Connector
1. _resolve_server(mode)

Выбор параметров подключения (login, password, server) в зависимости от режима:

LIVE — берёт из .env переменные MT5_LIVE_*

BACKTEST — берёт MT5_DEMO_*

2. _cleanup_cache_periodically(interval_minutes=60)

Фоновая очистка кэша (/data/cache/) каждые N минут в Live-режиме.
Запускается отдельным потоком.

3. _start_background_cache_cleaner()

Создаёт и запускает фоновый поток для _cleanup_cache_periodically.

4. connect_mt5(login, password, server)

Подключение к терминалу MetaTrader5:

Приведение login к int

Проверка статуса терминала (terminal_info)

Логирование ошибок и build номера терминала

5. disconnect_mt5()

Закрывает соединение с MT5 (mt5.shutdown()).

6. get_ohlcv(symbol, timeframe, start, end)

Получает OHLCV-данные через mt5.copy_rates_range()
Возвращает DataFrame со структурой:

time | open | high | low | close | volume

7. save_parquet(df, symbol, timeframe, mode)

Сохраняет DataFrame в .parquet:

Путь: /data/archive/{symbol}/{timeframe}/

Для Live — /data/cache/

8. validate_data(df, symbol)

Базовая проверка целостности:

отсутствие NaN

сортировка времени по возрастанию

(рекомендуется заменить на вызов data_integrity_checker.run_integrity_checks() для более строгого контроля)

9. clear_live_cache_on_start()

Очищает /data/cache/ при запуске Live-режима.

10. run_mt5_ingest(ctx)

Главная функция модуля:

Получает контекст Prefect (symbol, timeframe, date_start, date_end, mode)

Подключается к MT5

Загружает котировки

Проверяет качество (через validate_data)

Сохраняет результат

Логирует JSON-отчёт с status: SUCCESS / FAILED

======================
data_integrity_checker.py
Что такое 2.2.1.1 Data Integrity Checker

Это подмодуль внутри CoreData → DataIngest (MT5 Connector).
Он не загружает данные, а проверяет их качество перед сохранением —
чтобы в архив не попадали битые, неполные или дублированные котировки.

🧠 Зачем он нужен

Предотвращает ошибочные архивы.
Иногда MT5 отдаёт неполные бары или повторяющиеся timestamp’ы —
если это не проверять, ты потом получишь «скачущие» графики при бэктестах.

Экономит место и время.
Не нужно перезаписывать большие архивы, если проверка отсеяла брак заранее.

Контроль качества данных перед обучением ИИ.
При подготовке датасетов (для модуля 09_Learning_Module) тебе нужно быть уверен,
что данные без пропусков, отсортированы и унифицированы по временным зонам.