# ITA_TZ_03_DataStorage_v1.0.md  
**Модуль:** 03_DataStorage & Versioning  
**Статус:** базовое ТЗ на уровень модуля  
**Уровень:** без детализации внутренних реализаций подмодулей

## 1. Назначение модуля

Модуль **Data Storage & Versioning** обеспечивает:

1. Централизованное, консистентное и воспроизводимое хранение всех артефактов проекта ITA.
2. Однозначную привязку данных и результатов к:
   - `run_id`,
   - `setup_id`,
   - `rule_version`,
   - диапазону данных,
   - окружению и версии кода.
3. Управление жизненным циклом данных:
   - где лежат сырые данные,
   - где обработанные,
   - где результаты,
   - как очищаем старое.

**Главная идея:** в любой момент времени по одному `run_id` и `setup_id` можно восстановить:
- какие данные использовались,
- по каким правилам считали,
- каким кодом,
- какие артефакты получились.

Модуль обслуживает все остальные, но не содержит бизнес-логики анализа.

---

## 2. Границы ответственности

**Модуль ДЕЛАЕТ:**

- Хранение:
  - исторических и live данных (после ingest/processing);
  - JSON-выходов детекторов;
  - результатов бэктестов;
  - ссылок на PNG/HTML/annotated графики;
  - служебных метаданных (provenance).
- Версионирование:
  - правил/сетапов (связка с YAML),
  - запусков (run snapshot),
  - артефактов, связанных с конкретным запуском.
- Retention:
  - правила очистки временных и тяжёлых данных.

**Модуль НЕ ДЕЛАЕТ:**

- не выполняет детекторы;
- не принимает торговые решения;
- не оркестрирует флоу (это Prefect);
- не изменяет YAML-правила (управление — через процессы и другие ТЗ);
- не обучает модели.

Если какая-то логика «похожа на бизнес», её выносим в соответствующий модуль (CoreData, MarketTools, Backtester, Learning).

---

## 3. Связи с другими модулями

Модуль 03 работает как сервис для:

- **02_CoreData**  
  Сохраняет нормализованные данные и/или выдаёт пути к ним для повторного использования.

- **04_MarketTools**  
  Принимает от них структурированные JSON с детекциями для сохранения.

- **05_SetupManager**  
  Использует реестр правил и версий (модуль 03 предоставляет интерфейс для связи `setup_id` ↔ `rule_version` ↔ YAML).

- **06_BacktestManager**  
  Сохраняет результаты бэктестов, CSV сделок, ссылки на скрины, метаданные `run_id`.

- **07_Visualization** / **08_GPT_VisionFlow**  
  Используют единые контракты хранения для PNG/annotated PNG/JSON-аннотаций.

- **09_Learning_Module / ML Service**  
  Опирается на метаданные и артефакты для обучения и скоринга.

- **10_Outputs_Integrations** (Notion, Telegram)  
  Берёт готовые пути и метаданные, не строит их вручную.

- **11_Orchestration (Prefect Flows)**  
  На старте и окончании флоу регистрирует run’ы и связанные с ними артефакты через этот модуль.

**Ключевой принцип:**  
Ни один модуль не должен сам придумывать директории и схемы — он обращается к интерфейсам модуля 03.

---

## 4. Структура и соответствие ITA_modules_v1.1

Внутренняя логика модуля реализуется через подмодули, уже указанные в `ITA_modules_v1.1` для раздела Data Storage & Versioning:

1. **2.3.1 Persistent Store (Database / Object Storage)**  
   Хранение файлов и метаданных (candles, png, json, models и пр.).

2. **2.3.2 Rule & Version Registry**  
   Учёт версий правил/сетапов, их связи с файлами YAML.

3. **2.3.3 Run Snapshot / Provenance**  
   Запись параметров запусков, привязка артефактов к `run_id` и версиям.

4. **2.3.4 Data retention / purge policy**  
   Политики хранения и очистки старых данных.

---

## 5. Требования к данным и форматам (общие, модульные)

1. Все форматы хранения должны быть:
   - однозначны,
   - документированы,
   - согласованы с потоками данных (`ITA_Data_flows_v1.1.md`) и схемами.

2. Базовые форматы:
   - Табличные данные: `parquet` (предпочтительно), `csv` (по необходимости).
   - Структурированные данные: `json`.
   - Изображения: `png`.
   - Метаданные запусков, версий и связей: структурированные записи (json/БД) с явными ключами:
     - `run_id`, `setup_id`, `rule_version`, `symbol`, `timeframe`, `period`, `paths`.

3. Для каждого сохранённого результата:
   - Должна быть возможность восстановить входные данные и правила.
   - Должна быть явная связь с модулем-источником.

---

## 6. Требования к версии и provenance

Модуль обязан обеспечить:

1. Для **каждого backtest**:
   - сохранён `run_id`;
   - указаны `setup_id` и `rule_version`;
   - зафиксирован диапазон дат и инструмент;
   - есть ссылка на исходные данные (или их snapshot id);
   - есть ссылки на отчёты, PNG, таблицы сделок.

2. Для **live-сигналов**:
   - у каждого алерта есть `run_id`/`flow_id`;
   - логируется, по какому сетапу и версии он был сгенерирован.

3. Для **правил (YAML)**:
   - каждая опубликованная версия правила имеет уникальную запись;
   - можно найти, какие запуски были сделаны с этой версией.

4. Для **ML-модуля**:
   - есть возможность отследить, какие данные и сигналы использовались в обучении.

---

## 7. Требования к надёжности и эксплуатации

- Хранение локальное (MVP), с возможностью расширения на S3/облако без ломки интерфейсов.
- Операции записи/чтения:
  - логируются;
  - при ошибке не оставляют «битых» ссылок;
  - при частичном фейле результат должен быть отслеживаем через run/provenance.

---

## 8. Требования к реализации (без перечисления внутренних файлов)

1. Весь код модуля находится в:
   ```text
   src/03_DataStorage/
   ```
2. Все публичные функции модуля:
   - имеют понятные, документированные сигнатуры;
   - не зависят напрямую от UI (Streamlit, Telegram и т.п.);
   - не дублируют функциональность других модулей.

3. Для разработчиков:
   - в папке модуля должен быть `README.md` с кратким описанием:
     - роли модуля,
     - его контрактов,
     - примеров использования из других модулей.

---


---

## 🔄 Обновление 2025-11-09 (по результатам ITA_Dependency_Manifest v1.1)

Внесены изменения для обеспечения полной совместимости с модулем **02_CoreData**:

1. **Унификация путей в config.yaml**  
   Все ключи путей к данным объединены под единый блок:
   ```yaml
   paths:
     data_root: "data/"
     archive_root: "data/archive/"
     cache_root: "data/cache/"
     results_root: "data/results/"
   ```
   Это гарантирует, что и CoreData, и DataStorage используют одну схему путей.

2. **Добавление поля `run_id` в JSON Summary CoreData → DataStorage**  
   CoreData при экспорте данных теперь передаёт `run_id` в JSON метаданных, чтобы DataStorage мог корректно привязать provenance-запись.

3. **Добавление поля `rule_version` в DataStorage записи**  
   DataStorage теперь ожидает `rule_version`, передаваемый через SetupManager и CoreData, для однозначного связывания результатов с версиями правил.

4. **Логическая связь provenience**  
   Все артефакты в DataStorage фиксируют:
   - `run_id`
   - `setup_id`
   - `rule_version`
   - `symbol`, `timeframe`, `period`

5. **Изменение критериев принятия (Acceptance Criteria)**  
   Добавлен пункт:
   > Каждая запись об артефакте DataStorage должна содержать связку (`run_id`, `setup_id`, `rule_version`) в метаданных.

---

## 9. Acceptance Criteria (для всего модуля)

Модуль считается реализованным корректно, если:

1. Любой backtest или live-run:
   - создаёт запись о запуске;
   - сохраняет артефакты через единый набор функций Data Storage;
   - может быть полностью воспроизведён по `run_id`.

2. Любой сигнал/отчёт:
   - однозначно связан с `setup_id` и `rule_version`.

3. Ни один модуль:
   - не пишет «в обход» в произвольные пути;
   - не хранит собственные закрытые форматы, не согласованные с этим ТЗ.

4. Структура и поведение:
   - не противоречат `ITA_description.md`, `ITA_modules_v1.1.md`, `architecture_v1.1.md`,
     data-flows и Prefect-схемам,
     ITA Development Guidelines и manifest.
